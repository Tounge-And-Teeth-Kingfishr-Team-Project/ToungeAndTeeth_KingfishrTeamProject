using UnityEngine;

public class KeyManager : MonoBehaviour
{
    public bool yellowKey;
    public bool pinkKey;
    public bool blueKey;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
